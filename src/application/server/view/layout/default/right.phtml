<? if (C_Main::getPage() != "management"): ?>
<div class="span4">
    <div class="well">
        <?php if (C_Security_Authenticate::loginControl()):
        $user = C_Session::getUser();?>
        <h4 class="bottom-clear centered-text"><?= $user->getName(); ?></h4>
            <? if ($user->isApproved() == 1): ?>
        <? elseif ($user->isApproved() == 0): ?>
            <p><span class="label label-warning">Dikkat</span> Bu karakter henüz onaylanmamıştır!</p>
        <? else: ?>
            <p><span class="label label-important">Dikkat</span> Bu karakter reddedilmiştir!</p>
        <? endif; ?>
        <ul class="thumbnails">
            <li class="span1 centered">
                <div class="thumbnail">
                    <? $user->printSkin(55, 100); ?>
                </div>
            </li>
        </ul>
        <p>Yaş:<?= $user->getAge(); ?></p>
    <? else: ?>

    <? endif; ?>
    </div>
</div>
<? endif; ?>