<div class="span4">
    <div class="well">
        <?php if (C_Security_Authenticate::loginControl()):
        $user = C_Session::getUser();?>
        <h4><?= $user->getName(); ?></h4>
            <? if ($user->isApproved() == 1): ?>
        <p>Durum: <span class="label label-success">Onaylandı</span></p>
        <? elseif ($user->isApproved() == 0): ?>
        <div class="alert alert-block">
            <p>Durum: <span class="label label-warning">Beklemede</span></p>
            
        </div>
        <? else: ?>
            <p>Durum: <span class="label label-important">Reddedildi</span></p>
        <? endif; ?>
    <? else: ?>

    <? endif; ?>
    </div>
</div>